package org.example.wtp.handler;

import org.apache.ibatis.session.ResultContext;
import org.apache.ibatis.session.ResultHandler;
import org.example.wtp.domain.CarSpeed;

public class CarSpeedResultHandler implements ResultHandler<CarSpeed> {

    @Override
    public void handleResult(ResultContext<? extends CarSpeed> resultContext) {

    }
}
